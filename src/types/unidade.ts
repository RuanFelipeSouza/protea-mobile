/**
 * Representação canônica de uma Unidade de atendimento.
 * O campo `unidade` mantém o nome enviado pela API (`codedatas/unidadesPrest`).
 */
export type Unidade = {
  id: number
  unidade: string
}
